import { getMetadata } from "~~/utils/scaffold-eth/getMetadata";

export const metadata = getMetadata({
  title: "LSP17 Stealth Extension Debug",
  description: "Test and debug the LSP17StealthExtension implementation for stealth addresses on LUKSO",
});
